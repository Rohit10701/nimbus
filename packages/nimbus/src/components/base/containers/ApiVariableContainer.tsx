import React from 'react';
const ApiVariableContainer = () => {
  return (
    <div
      className="w-full h-full flex-1 bg-red-400 flex flex-grow-0 justify-center items-center px-1"
      style={{
        border: 'var(--border-width) solid var(--color-border)',
        borderRadius: 'var(--border-radius)',
      }}
    >
      ApiVariableContainer
    </div>
  );
};

export default ApiVariableContainer;